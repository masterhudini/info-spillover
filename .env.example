# Environment variables for info_spillover project

# MLFlow Configuration
MLFLOW_TRACKING_URI=file:./mlruns
MLFLOW_DEFAULT_ARTIFACT_ROOT=./mlruns
MLFLOW_EXPERIMENT_NAME=info_spillover_experiment

# DVC Configuration
DVC_REMOTE_URL=/home/Hudini/gcs
DVC_REMOTE_NAME=gcs_bucket

# Data Configuration
DATA_PATH=data/
RAW_DATA_PATH=data/raw/
PROCESSED_DATA_PATH=data/processed/
MODELS_PATH=models/saved/

# Logging Configuration
LOG_LEVEL=INFO
LOG_FILE=experiments/logs/experiment.log

# Model Configuration
MODEL_REGISTRY_URI=
DEFAULT_MODEL_STAGE=Production

# External APIs (if needed)
API_KEY=
API_URL=

# Database Configuration (if needed)
DATABASE_URL=
DB_HOST=localhost
DB_PORT=5432
DB_NAME=info_spillover
DB_USER=
DB_PASSWORD=

# Cloud Storage (if using)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-west-2
S3_BUCKET_NAME=

# Google Cloud (if using)
GOOGLE_APPLICATION_CREDENTIALS=
GCP_PROJECT_ID=
GCS_BUCKET_NAME=

# Azure (if using)
AZURE_STORAGE_CONNECTION_STRING=
AZURE_CONTAINER_NAME=

# Development flags
DEBUG=false
DEVELOPMENT_MODE=false